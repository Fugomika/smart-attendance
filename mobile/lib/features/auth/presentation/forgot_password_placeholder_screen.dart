import '../../../shared/widgets/placeholder_screen.dart';

class ForgotPasswordPlaceholderScreen extends PlaceholderScreen {
  const ForgotPasswordPlaceholderScreen({super.key})
    : super(
        title: 'Lupa Password',
        description: 'UI lupa password akan diimplementasikan pada batch auth.',
      );
}
